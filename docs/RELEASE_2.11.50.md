# 52nm Android v2.11.50

- 精确流量统计：读取核心原始累计上下行字节分别上报，界面缩放数值不再作为计费依据。
- 修复流量额度用尽后恢复时，第一段流量被服务端当作重复上报而丢弃的问题。
  服务端在「本次上报已入账后才判定超额」与「上报前就已超额」两种情况下都返回同一错误码，
  旧版本未加区分，恢复后会沿用已被接收过的序号，导致这段流量不计入统计。
- 发布同步：新增独立的 NAS Android 发布同步流程，支持断点续传、重试与固定别名校验。
